[package]
name = "radixdb-sdk-proof-plugin"
version = "1.0.0"
edition = "2021"
publish = false
license = "Apache-2.0"

[lib]
crate-type = ["cdylib"]

[dependencies]
radixdb-plugin = { path = "../../.." }

[workspace]
